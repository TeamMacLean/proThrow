<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>TSL Proteomics</title>

    <link rel="icon" type="image/x-icon" href="/favicon.ico?v=2">
    <link href="/css/main.css" rel="stylesheet">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css"
          integrity="sha384-DNOHZ68U8hZfKXOrtjWvjxusGo9WQnrNx2sqG0tfsghAvtVlRW3tvkXWZh58N9jp" crossorigin="anonymous">


</head>

<body>


<nav class="navbar navbar-expand-lg navbar-expand-md navbar-dark bg-primary mb-4">
    <div class="container">
        <a class="navbar-brand" href="/">
            <img src="/logo.png" width="30" height="30" class="d-inline-block align-top" alt="">
            TSL Proteomics
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <% if(locals.signedInUser){ %>
                <li class="nav-item">
                    <a class="nav-link" href="/user/<%- locals.signedInUser.username %>">My Requests</a>
                </li>
                <% } %>
            </ul>
            <ul class="navbar-nav">

                <% if(locals.signedInUser){ %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown"
                       role="button" data-toggle="dropdown"
                       aria-haspopup="true" aria-expanded="false">
                        <%- locals.signedInUser.name %>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <% if(locals.signedInUser.isAdmin){ %>
                        <a class="dropdown-item" href="/admin">Admin</a>
                        <div class="dropdown-divider"></div>
                        <% } %>
                        <a class="dropdown-item" href="/signout">Sign Out</a>
                    </div>
                </li>
                <% } else { %>
                <li class="nav-item active">
                    <a class="nav-link" href="/signin">Sign in</a>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
